import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  registerButton: {
    marginTop: 50,
    width: '75%',
    alignSelf: 'center',
    justifyContent: 'center',
    borderRadius: 20,
  },
  buttonText: {
    fontWeight: 'bold',
    fontSize: 21,
  },
});
