import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},
  contentContainer: {},
  chartContainer: { marginTop: 5, borderWidth: 1 },
  chart: {
    margin: 0,
  },
});
