export interface TrainModule {
  moduleName: string;
  // levels: TrainLevel[]; : Not using levels atm
  moduleGroup: string;
  moduleContent: string;
  // new fields
  thumbnail: string;
  desc: string;
}
